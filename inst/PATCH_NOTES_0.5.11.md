# Patch notes: personnelSelectionUtility 0.5.11

This patch updates the pkgdown site appearance and navigation.

## Changes

- Uses Bootswatch Flatly with Bootstrap 5 in `_pkgdown.yml`.
- Uses explicit navbar entries rather than the previous structural navbar shorthand.
- Adds `pkgdown/extra.css` for small visual refinements.
- Keeps the same four vignettes and the same statistical API.

## Computation

No computational functions were changed.
