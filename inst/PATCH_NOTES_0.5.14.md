# Patch notes for personnelSelectionUtility 0.5.14

This is a documentation-site presentation patch.

Changes:

- Renamed the pkgdown navbar item `New` to `News`.
- Updated `pkgdown/extra.css` to hide the automatic navbar logo so the logo appears only once at the beginning of the README and vignette content.
- Updated package metadata and citation version to 0.5.14.

No statistical computations were changed.
