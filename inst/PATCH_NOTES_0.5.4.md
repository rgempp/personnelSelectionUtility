# Patch notes: personnelSelectionUtility 0.5.4

This patch fixes a pkgdown site-building error caused by an exported topic, `inflation_adjusted_rate()`, being absent from the `_pkgdown.yml` reference index.

No computational routines were changed.
