% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dominance.R
\name{dominance_analysis}
\alias{dominance_analysis}
\title{Dominance analysis for predictor importance}
\usage{
dominance_analysis(predictor_cor, predictor_criterion_cor)
}
\arguments{
\item{predictor_cor}{Predictor correlation matrix \code{R_xx}.}

\item{predictor_criterion_cor}{Vector of predictor-criterion correlations
\code{r_xy} (length \code{p}).}
}
\value{
A list with components:
\describe{
\item{r_squared_full}{The full-model \code{R^2}.}
\item{general_dominance}{Vector of length \code{p} whose entries sum to
\code{r_squared_full}.}
\item{conditional_dominance}{\verb{p x p} matrix; row \code{i} gives the average
contribution of predictor \code{i} at subset sizes \verb{0, 1, ..., p-1}.}
\item{complete_dominance}{\verb{p x p} logical matrix where entry \verb{[i,j]}
is \code{TRUE} if \code{i} completely dominates \code{j}, \code{FALSE} if \code{j} completely
dominates \code{i}, \code{NA} otherwise.}
}
}
\description{
Implements Budescu's (1993) dominance analysis to decompose the
coefficient of determination of a multiple regression into contributions
attributable to each predictor. Three dominance summaries are returned:
}
\details{
\itemize{
\item \strong{Complete dominance}: predictor \code{i} \emph{completely dominates} \code{j}
if \verb{R^2(S \\cup \{i\}) > R^2(S \\cup \{j\})} for every subset \code{S} not
containing \code{i} or \code{j}. Reported as a pairwise dominance matrix.
\item \strong{Conditional dominance}: average increment of predictor \code{i} to
\code{R^2} across subsets of size \code{k}, for \verb{k = 0, ..., p-1}.
\item \strong{General dominance}: the average of conditional dominance
values; equivalent to the Shapley value of \code{R^2}.
}
}
\examples{
Rxx <- matrix(c(1, .30, .20,
                .30, 1, .25,
                .20, .25, 1), 3, 3)
rxy <- c(.40, .30, .25)
dominance_analysis(Rxx, rxy)
}
\references{
Azen, R., & Budescu, D. V. (2003). The dominance analysis approach for
comparing predictors in multiple regression. \emph{Psychological Methods}, 8,
129-148.

Budescu, D. V. (1993). Dominance analysis: A new approach to the problem
of relative importance of predictors in multiple regression.
\emph{Psychological Bulletin}, 114, 542-551.
}
