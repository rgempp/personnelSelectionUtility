% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fuse-helpers.R
\name{fuse_composite_cor}
\alias{fuse_composite_cor}
\title{Correlation matrix between several weighted composites}
\usage{
fuse_composite_cor(weights_matrix, item_cor)
}
\arguments{
\item{weights_matrix}{\verb{p x m} matrix; column \code{j} is the weight vector of
composite \code{j}.}

\item{item_cor}{\verb{p x p} correlation matrix among items.}
}
\value{
\verb{m x m} correlation matrix among composites.
}
\description{
Given a stack of items and a weight matrix \code{W} whose columns are
composite-specific weight vectors, computes the correlation matrix
between the resulting composites under the standard Lord-Novick formula.
}
\examples{
R <- diag(4); R[lower.tri(R)] <- R[upper.tri(R)] <- .25
W <- cbind(c(1, 1, 0, 0), c(0, 0, 1, 1))
fuse_composite_cor(W, R)
}
