% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/offer-rejection.R
\name{offer_rejection_adjustment}
\alias{offer_rejection_adjustment}
\title{Offer-rejection adjustment for selection utility (Murphy, 1986)}
\usage{
offer_rejection_adjustment(
  expected_z_offered,
  mode = c("uniform", "selective", "correlated"),
  acceptance_rate = 1,
  rho_quality_acceptance = 0,
  logit_intercept = NULL,
  logit_slope = NULL,
  n_offered = NULL
)
}
\arguments{
\item{expected_z_offered}{Expected standardized score of offered
candidates (e.g., \code{selected_mean_z(selection_ratio)}).}

\item{mode}{One of \code{"uniform"}, \code{"selective"}, or \code{"correlated"}.}

\item{acceptance_rate}{Expected proportion of offers accepted (used in
all three modes for the headcount-scaling output).}

\item{rho_quality_acceptance}{Correlation between standardized candidate
quality and acceptance propensity (used for \code{mode = "correlated"}).
Negative values reflect adverse selection (top candidates more likely
to decline).}

\item{logit_intercept, logit_slope}{Logit link coefficients for
\code{mode = "selective"}. The slope is typically negative for adverse
selection.}

\item{n_offered}{Optional integer; if supplied, the function also returns
the expected number of accepted hires.}
}
\value{
A list with \code{expected_z_accepted}, \code{acceptance_rate},
\code{effective_validity_loss} (the difference between offered and accepted
means), and optionally \code{expected_n_accepted}.
}
\description{
Adjusts the expected standardized criterion score of accepted hires when
offer recipients can decline. When the probability of accepting an offer
is \emph{negatively} correlated with candidate quality (top candidates have
more outside options), the realized mean criterion of accepted hires is
below the mean of selected (offered) candidates.
}
\details{
Three modes are supported:
\itemize{
\item \code{mode = "uniform"}: a fixed acceptance probability \code{p} independent
of candidate quality. The expected criterion of accepted hires equals
the expected criterion of those offered, but the realized headcount is
scaled by \code{p}.
\item \code{mode = "selective"}: the probability of acceptance depends on
candidate standardized quality \code{z} through a logit link
\code{logit(p) = a + b * z} with \code{b < 0} for adverse selection. The
adjusted mean criterion is computed by integrating the standard
normal weighted by the acceptance probability.
\item \code{mode = "correlated"}: a closed-form approximation under the
assumption that quality and acceptance are jointly normal with
correlation \code{rho_quality_acceptance}. The adjustment is
\eqn{\bar{z}_{accepted} \approx \bar{z}_{offered} + \rho \cdot
    (\lambda(z_p) - \bar{z}_{offered})} for an acceptance threshold
\code{z_p} derived from the expected acceptance rate.
}
}
\examples{
z_offered <- selected_mean_z(0.20)

# Uniform 70\% acceptance rate, no quality dependence:
offer_rejection_adjustment(z_offered, mode = "uniform",
                           acceptance_rate = 0.70, n_offered = 100)

# Adverse selection: top candidates more likely to decline.
offer_rejection_adjustment(z_offered, mode = "correlated",
                           acceptance_rate = 0.70,
                           rho_quality_acceptance = -0.20,
                           n_offered = 100)
}
\references{
Hogarth, R. M., & Einhorn, H. J. (1976). Optimal strategies for personnel
selection when candidates can reject job offers. \emph{Journal of Business},
49, 479-495.

Murphy, K. R. (1986). When your top choice turns you down: Effect of
rejected offers on the utility of selection tests. \emph{Psychological
Bulletin}, 99, 133-138.
}
